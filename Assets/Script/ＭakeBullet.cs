using UnityEngine;
using System.Collections;

public class MakeBullet:MonoBehaviour{
/*
    public GameObject obj_Bullet;
    public float f_Time;
    public float f_timeToShoot;

    void Start()
    {
        f_Time=0;
    }
    void Update(){
        f_Time+=f_Time.deltaTime;
        if(Input.GetMouseButtonDown(0))
        {
            Fire();
        }
    }
    void Fire()
    {
        if(f_Time>=f_timeToShoot)
        {
            GameObject obj_CloneBullet=Instantiate(obj_Bullet) as GameObject;
            Vector3 Math_Point = Camera.main.ScreenToWorldPoint(new Vector3(Input.mousePosition.x,Input.mousePosition.y,10));
            obj_CloneBullet.transform.position = Math_Point;
        }
    }


*/
}